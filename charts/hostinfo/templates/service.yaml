apiVersion: v1
kind: Service
metadata:
  name: {{ template "hostinfo.fullname" . }}
  labels:
    app: {{ template "hostinfo.name" . }}
    chart: {{ template "hostinfo.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hostinfo.name" . }}
    release: {{ .Release.Name }}
